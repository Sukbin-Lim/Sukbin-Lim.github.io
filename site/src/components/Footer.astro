---
import { profile } from '../data/profile';
import { publications } from '../data/publications';
import { fabricatedChipCount } from '../data/projects';
---
<div class="foot">
  <span>
    {profile.name} · KAIST EE · HyperAccel ·
    <a class="foot-a" href={profile.scholar} target="_blank" rel="noopener">Google Scholar</a>
  </span>
  <span>{fabricatedChipCount} fabricated chips · {publications.length} papers</span>
</div>
